description "Apereo CAS Management Submissions Config"

dependencies {
    apply from: rootProject.file("config/delegated-config.gradle")

    implementation project(":config:cas-mgmt-config-submissions")
}